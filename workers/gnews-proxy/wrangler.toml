name = "md-gnews-proxy"
main = "src/index.ts"
compatibility_date = "2024-04-01"

# Plain-text vars (dashboard or --var): comma-separated origins, or "*" for any origin.
# [vars]
# ALLOWED_ORIGINS = "*"

# Optional secret — masks client key: `wrangler secret put GNEWS_API_KEY`
